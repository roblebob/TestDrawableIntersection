package com.example.testdrawableintersection;

import android.graphics.Path;
import android.graphics.PathMeasure;
import android.graphics.Path.Op;
import android.graphics.drawable.VectorDrawable;

public class IntersectionUtils {

    public static Path findIntersectionPoints(VectorDrawable drawable1, VectorDrawable drawable2) {
        // Get the PathData from the VectorDrawables

 /*       drawable1.getOutline().getOutline(path1);

        Path path1 = new Path().;
        Path path2 = new Path();
        drawable1..getOutline(path1);
        drawable2.getOutline().getOutline(path2);

        // Find the intersection points
        Path intersectionPath = new Path();
        intersectionPath.op(path1, path2, Op.INTERSECT);

        return intersectionPath;*/
        return null;
    }
}
